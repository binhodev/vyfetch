import { describe, it, expect } from "vitest";

describe("VyTypes", () => {
    it("VyFetchResponse should be a valid type (compile-time check only)", () => {
        expect(true).toBe(true);
    });
});
